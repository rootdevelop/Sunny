namespace Sunny.Core.Services
{
    public static class CustomServiceBindings
    {
        public static void RegisterBindings()
        {
            //Cirrious.CrossCore.Mvx.RegisterSingleton<ILocalStorageService>(() => new LocalStorageService());
        }
    }
}
